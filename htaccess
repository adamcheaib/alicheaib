<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Only redirect if the file or directory doesn't exist
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # Send everything else to index.html
  RewriteRule ^ index.html [L]
</IfModule>

